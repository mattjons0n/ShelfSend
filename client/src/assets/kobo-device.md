# Kobo connection photo

`kobo-device.png` is a transparent, display-sized derivative of the Kobo photo supplied by the user on 2026-09-14. The original upload is unchanged.

Prepared with the built-in image-editing tool, then downscaled to 192 pixels while preserving alpha. Used in the Kobo connection chooser and connected status; no background tile is applied.

## Image-editing prompt

Use case: background-extraction

Asset type: Kobo connection icon in a private ebook library web app

Input image: Image 1 is the edit target: the supplied photograph of a black Kobo e-reader.

Primary request: Remove only the pale background outside the e-reader, producing a clean photo cutout on an actually transparent alpha background. Preserve the complete device exactly, including its rounded black bezel, screen contents, Kobo logo, proportions, colors, texture, perspective and orientation. Do not redraw or restyle the device or its screen. Clean antialiased edges, no pale fringe, no added shadow. Tight portrait framing with a small transparent margin and the entire device visible. Output a transparent PNG, no checkerboard drawn into the pixels.
